require("FairyGUI")

local g = {}

---@type FGUIWindowLua
local self = self

--窗体初始化时
function onInit()
    -- FGUI组件
    ---@type FairyGUI.GComponent
    g.contentPane = self.contentPane

end

--被调用 window:InitByCustomDatas() 时
function onCustomInit()
    -- 自定义数据
    -- 类型为 any 可以是任何类型
    g.CustomData = self.CustomData
    g.CustomArgObj1 = self.CustomArgObj1
    g.CustomArgObj2 = self.CustomArgObj2
    g.CustomArgObj3 = self.CustomArgObj3
end

--窗体开始显示并播放显示动画时
function onDoShowAnimation()

end

--窗体播放完显示动画时
function onShown()

end

--窗体开始播放关闭动画时
function onDoHideAnimation()

end

--窗体隐藏时
function onHide()

end

--战斗界面脚本 绑定战场
---@param battleScene BattleSceneScript 战场脚本
function SetBattleScene(battleScene)

end

--设置窗口参数
--被调用 window:SetWindowParams(arg1.arg2,...)
---@param args string[] 参数数组
function SetWindowParams(args)

end

--UI消息事件
---@param type string 消息类型
---@param arg string 消息参数
function onWindowEvent(type, arg)

end

--按钮按下事件
---@param keyCode number 按键代码，参考Unity的KeyCode枚举
function onKeyDown(keyCode)

end